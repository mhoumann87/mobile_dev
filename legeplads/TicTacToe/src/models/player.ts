export class Player {

    playerName: string;
    playerWon: number;

 constructor(playerName: string, playerWon: number) {

     this.playerName = playerName;
     this.playerWon = playerWon;
    }


}