export class Board {

    brand:  string;
    model: string;
    color: string;
    price: number;

    constructor(brand: string, model: string, color: string, price: number ) {

        this.brand = brand;
        this,model = model;
        this.color = color;
        this.price = price;
    }
}