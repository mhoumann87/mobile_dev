export class Bearings {

    noInPackage: number;
    bearingType: string;
    bearingPrice: number;

constructor(noInPackage: number, bearingType: string, bearingPrice: number) {

    this.noInPackage = noInPackage;
    this.bearingType = bearingType;
    this.bearingPrice = bearingPrice;
}
}
