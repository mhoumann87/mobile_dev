export class Wheels {

    diameter: number;
    wheelColor: string;
    hardness: string;
    wheelPrice: number;

    constructor(diameter: number, wheelColor: string, hardness: string, wheelPrice) {

        this.diameter = diameter;
        this.wheelColor = wheelColor;
        this.hardness = hardness;
        this.wheelPrice = wheelPrice;
    }
}